# 二进制数据库文件，不纳入 git
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# 飞书同步增量水位（每设备独立，不纳入 git）
data/feishu_sync_state.json

# 临时脚本
scripts/bulk_*.py
scripts/_*.py

# 生成的 HTML 报告（每次重新生成即可）
scripts/kol_analysis_*.html

# Python 缓存
__pycache__/
*.pyc
